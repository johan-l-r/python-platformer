# Improving movement
This log may be a little short, because I do not have that much to explain here since I don't fully understand it myself. 

r reimplemented the movement an jumping logic using Pygame's 2D vectors, and applying some basic physics concepts like: acceleration and velocity. The reason I say I don't fully understand the code, is because **currently** I don't have the knowledge of how velocity and acceleration are interacting. 

However, I don't like the feeling of not knowing something, so I plan to learn more about physics not only because I don't know how my code works, but because it is an interesting topic.

For now, this is the new reimplementation: 

```python
def move(self, keys): 
  self.acceleration = pg.math.Vector2(0, self.GRAVITY)
  self.direction.x = 0

  if keys[pg.K_a]: 
    self.direction.x = -1
  elif keys[pg.K_d]: 
    self.direction.x = 1
  else: 
    self.direction.x = 0

  self.acceleration.x = self.SPEED * self.direction.x
  self.acceleration.x -= self.velocity.x

  self.velocity += self.acceleration
  self.position += self.velocity

  self.rect.bottomleft = self.position

def jump(self, keys): 
  if keys[pg.K_SPACE] and self.is_on_ground:
    self.velocity.y = -self.JUMP_FORCE
    self.is_on_ground = False

```
